language: python
python: "2.7"

before_install:
  - sudo apt-get update -qq

install:
  - pip install ansible

script:
  - ansible-playbook modern.yml --syntax-check
  - ansible-playbook modern.yml -l travis_ci
  - bash -lec "gradle check cucumber"
